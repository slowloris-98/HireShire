- Add more valid companies from greenhouse, ashby and lever.
- Filter companies
- A jobs shortlisted from matcher for a single days should be added in a csv with job link and folder link to resume if any.
    - if no resume from tuner then atleast the entry should be there
    


once pipeline is finished the final pipeline json / csv should be accesible from my phone

have the matcher env provider also in the config

