- emperical study on the scrapped jobs and the matched job in the last run. increase the thereshold and identify the sweet spot.


- Funnel at matcher using bert or minilm based on the job title.
- we can also just fetch the job title first and then use the funnel to see if job is relevant or not, and then fetch the details
    - move detail api in the matcher


- so all the location facets for each workday is different and we might need to maintain applied facet json for each company.

- insert google, microsoft and stand alone big tech
    - make a skill for job collection of these companies

- how are the slugs being collected

- once pipeline is finished the final pipeline json / csv should be accesible from my phone


- 2 phase Tuner sounds good, but maybe one shot resume tuner maybe more efficient.
    - because theevaluator will reason why not hire, but as llm when the tokens of why not enter the system it will unintentionally include those things.
    - the skieing example, focus on the path and not the trees.
    - experiment with a couple of prompts and approaches. need to get hit rate like manav.





add evals:
1. Review the tuned resume LLM-as-a-judge
2. Check mail and look for conversion rate?